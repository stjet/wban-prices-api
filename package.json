{
  "private": true,
  "name": "wban-prices-api",
  "version": "1.0.0",
  "description": "yuuup",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  }
}
